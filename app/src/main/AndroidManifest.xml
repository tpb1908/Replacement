<manifest package="com.tpb.timetable"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme">

        <activity
            android:name=".Home.Home"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".Home.Input.HomeworkInput"
            android:taskAffinity=""
            android:theme="@style/Theme.Sliding.Light">
        </activity>
        <activity
            android:name=".Home.Input.AssessmentInput"
            android:taskAffinity=""
            android:theme="@style/Theme.Sliding.Light">
        </activity>
        <activity
            android:name=".Home.Input.ReminderInput"
            android:taskAffinity=""
            android:theme="@style/Theme.Sliding.Light">
        </activity>
        <activity
            android:name=".Setup.Input.TermInput"
            android:taskAffinity=""
            android:theme="@style/Theme.Sliding.Light"
            >

        </activity>
        <activity android:name=".Home.Splash"
                  android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>
